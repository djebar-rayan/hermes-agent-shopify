# <STORE_NAME> Facts (Hermes persistent memory)

> Copy this file to `$HERMES_WORKSPACE/MEMORY.md` and fill it in with your store's permanent facts.
> Keep it compact (< 60 lines recommended). For detailed history, use a separate `MEMORY-HISTORY.md`.

## Store
- Shop: <store_handle>.myshopify.com (Shopify <plan>, <currency>), custom domain <store>.com
- ~<N> active products / <N> collections / <N> pages
- <country/region> brand, <brand theme> universe.
- Instagram: @<handle> (<N> followers, <N> posts). Posting slot: <Paris hours or other>.

## Brand vocabulary (mandatory)
See `STORE-BRAND.md` rule 6. Summary: <list of 10-20 keywords>.

## VPS Stack
- Ubuntu 24.04, Node 20+, Python 3.11+, Shopify CLI 3.94+
- Workdir: `$HERMES_WORKSPACE` (= /root/<store>-shopify)
- Hermes v0.14.0, OpenViking 0.3.16 (port 1933)
- <N> crons: weekly-perf-report, weekly-ideas, weekly-meta-review, watchdog-conversion
- Hooks v0.14: post_tool_call (log-learning) + on_session_start (inject-standing)
- 25 skills (21 shopify-* + 4 hermes-*)

## LLM Model
- Primary: `google/gemini-3.1-flash-lite-preview` via OpenRouter
- Alternatives: <depending on your setup>
- Context 262144 tokens, timezone <your timezone>

## Credentials .env (names only, values in /root/.hermes/.env chmod 600)
- OPENROUTER_API_KEY, KLAVIYO_API_KEY, SHOPIFY_STORE, SHOPIFY_CLI_THEME_TOKEN
- GEMINI_API_KEY, OPENAI_API_KEY, FIRECRAWL_API_KEY, GSC_SERVICE_ACCOUNT
- SMTP Gmail (<your_email> sender and recipient)
- TELEGRAM_BOT_TOKEN, TELEGRAM_ALLOWED_USERS, TELEGRAM_HOME_CHANNEL

## Current phase: TEST EXCLUSIVE
- `HERMES_MODE=test` in .env (test = apply+rollback within the same run, prod = persistent)
- `HERMES_TEST_EMAIL_TO=<your_email>`
- Switch to prod = operator decision after test-phase validation (see docs/PRODUCTION-CHECKLIST.md)

## Active integrations
- Shopify Admin API (OAuth + Theme Access)
- Klaviyo (read-only via lib/klaviyo-fetch.sh, 6h cache) — <N> flows, <N> campaigns
- Google Search Console (if configured)
- Firecrawl (competitive monitoring)
- SMTP Gmail (sending via inline Python snippet)
- Telegram Bot (/yes validation, watchdog alerts)

## Hermes autonomy levels (summary — detail in STORE-BRAND.md)
- 🟢 alone: <list of 🟢 actions>
- 🟡 operator validation: <list of 🟡 actions>
- 🔴 never alone: <list of 🔴 actions>

## Open decisions
- <e.g.: Switch to prod, configure Klaviyo, add X skill, ...>

For detailed history (install phases, Grand Run, bugs fixed, etc.): see `learnings.md` + `MEMORY-HISTORY.md`.
